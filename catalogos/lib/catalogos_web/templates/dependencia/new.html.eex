<h1>Agregar dependencia.</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.dependencia_path(@conn, :create)) %>

<span><%= link "Regresar", to: Routes.dependencia_path(@conn, :index) %></span>
